# 環境変数（認証情報を含むため絶対にコミットしない）
.env

# データベースファイル（Supabaseを使うので不要）
*.db

# 実行ファイル
*.exe
# server ← これを消して、下の行のように具体的に書く
backend/cmd/server/server

# データファイル（ローカルで使う用）
data/*.csv
data/*.xlsx

# 依存関係
vendor/